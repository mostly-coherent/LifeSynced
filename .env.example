# LifeSynced Calendar Sync Configuration
# Copy this file to .env.local at the repo root (LifeSynced/.env.local).
# Python sync scripts and the calendar-ui Next.js app both read from this file.

# ============================================
# Calendar UI (Next.js app in calendar-ui/)
# ============================================
# Password protection for the web app
APP_PASSWORD=your-app-password
# Optional: separate password for "personal reveal" feature
# PERSONAL_REVEAL_PASSWORD=your-reveal-password

# Supabase (for calendar-ui if using cloud storage)
# NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# ============================================
# Microsoft Graph API (Work Outlook Calendar)
# ============================================
# Required for sync_calendar.py (Graph API method)
# Get these from Azure Portal → App registrations → Your app
CLIENT_ID=your-azure-client-id-here
TENANT_ID=your-azure-tenant-id-here

# ============================================
# Outlook ICS Feed (Alternative Method)
# ============================================
# Required for sync_calendar_ics.py (ICS feed method - no admin consent needed)
# Get this from Outlook on the Web → Calendar → Settings → Shared calendars → Publish
OUTLOOK_ICS_URL=https://outlook.office.com/owa/calendar/your-calendar-id.ics

# ============================================
# Apple Calendar / iCloud (Personal Calendars)
# ============================================
# Option 1: iCloud.com Public Calendar Link (Recommended)
# Get from iCloud Calendar → Share → Public Calendar → Copy ICS link
# For multiple calendars, use comma-separated URLs
APPLE_CALENDAR_ICS_URL=https://p...calendar.icloud.com/...

# Option 2: ICS File Export (Alternative)
# Path to exported .ics file(s), comma-separated for multiple calendars
# APPLE_CALENDAR_ICS_PATH=/path/to/calendar1.ics,/path/to/calendar2.ics

# Option 3: Direct Database Access (Fallback - macOS only)
# Only works if iCloud calendars are synced locally to macOS Calendar app
# APPLE_CALENDAR_DB_PATH=~/Library/Calendars/Calendar.sqlite

# ============================================
# Database Configuration
# ============================================
# Path to SQLite database (default: calendar.db in project root)
DB_PATH=calendar.db

# ============================================
# Advanced Options
# ============================================
# Skip Graph API sync (useful when waiting for admin consent)
# Set to 'true' to disable Graph API sync in sync_all_calendars.py
SKIP_GRAPH_API=False
