# Supabase Configuration (Required)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Calendar ICS URLs (Optional - at least one recommended)
# Outlook ICS feed URL (get from Outlook on the Web → Calendar → Share → ICS link)
OUTLOOK_ICS_URL=

# iCloud Calendar ICS URLs (comma-separated for multiple calendars)
# Get from iCloud.com → Calendar → Share → Public Calendar
APPLE_CALENDAR_ICS_URL=

# Microsoft Graph API (Optional - for richer event details)
# Requires Azure App Registration with admin consent
# CLIENT_ID=
# TENANT_ID=

